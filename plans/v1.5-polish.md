# V1.5 — Polish Pass

## Objective
Complete the remaining V1 plan items that were scoped but not built. Focus on UX completeness.

## Tasks (priority order)

### 1. Inline notes editing in SidePanel
- Notes section becomes editable textarea on click
- Auto-save on blur via PUT /api/concepts/:id
- Acceptance: click notes -> edit -> blur -> persisted

### 2. Streaming responses in ChatPanel
- Use Ollama streaming API (already have generate_stream in ai.py)
- Add SSE or chunked response endpoint
- Frontend renders tokens as they arrive
- Acceptance: typing a question shows answer streaming word-by-word

### 3. Conversation history in ChatPanel
- Show previous Q&A from conversations table
- Load on panel open, newest first
- Click a past question to see its answer
- Acceptance: past conversations persist across panel open/close

### 4. Node size scaling with connection count
- Nodes with more edges render larger
- Scale factor: base + (edge_count * multiplier)
- Acceptance: hub nodes visually stand out

### 5. Hover tooltip on graph nodes
- Show summary on mouseover (not just highlight)
- Use Cytoscape popper or custom tooltip div
- Acceptance: hovering a node shows its summary

### 6. Right-click context menu on nodes
- Options: delete, enrich, connect to...
- Acceptance: right-click shows menu, actions work

### 7. Double-click node to edit
- Opens concept in edit mode (name, category, tags, notes)
- Acceptance: double-click -> modal with editable fields -> save

### 8. Enrichment progress in AddModal
- Show status: "fetching docs... summarizing... done"
- Poll or SSE for background task status
- Acceptance: user sees enrichment progress after adding

### 9. Menu bar
- File: Add concept, Export graph (JSON)
- View: Fit graph, Toggle sidebar
- Help: About, Keyboard shortcuts
- Acceptance: menu bar works in Tauri window
